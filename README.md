# quote-generator-with-history
A full-stack Quote Generator with History built using HTML, CSS, JavaScript, Python (Flask), SQLite, and an external Quotes API. The application fetches random quotes, stores them in a database, and displays a history of previously generated quotes.
